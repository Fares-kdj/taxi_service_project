# 🚖 نظام حجز وإدارة سيارات الأجرة (Taxi Oran)

نظام متكامل لحجز وإدارة سيارات الأجرة مصمم خصيصاً لولاية **وهران، الجزائر**. يتيح هذا المشروع للمستخدمين حجز سيارات الأجرة بسهولة عبر الإنترنت، مع حساب التكلفة التقديرية للرحلة، ويوفر لوحة تحكم شاملة للمسؤولين لإدارة الحجوزات والسائقين والأسعار.

## ✨ المميزات الرئيسية

### 👤 للمستخدمين:
- **حجز سهل وسريع**: واجهة مستخدم بديهية لحجز الرحلات في خطوات بسيطة.
- **تعدد اللغات**: دعم كامل للغتين العربية والفرنسية.
- **تقدير الأسعار**: حساب تكلفة الرحلة بناءً على المسافة ونوع السيارة المختار.
- **أنواع خدمات متعددة**: خيارات متنوعة (سيارة عادية، VIP، نقل عائلي، إلخ).
- **تصميم متجاوب**: يعمل بكفاءة على الهواتف المحمولة والأجهزة اللوحية والحواسيب.
- **نموذج تواصل**: للتواصل المباشر مع الإدارة.

### 🛡️ للمسؤولين (لوحة التحكم):
- **إدارة الحجوزات**: عرض وتعديل وإلغاء الحجوزات الواردة.
- **إدارة الأسطول**: إضافة وتعديل السيارات والسائقين.
- **إدارة الأسعار**: تحديد أسعار الكيلومتر والدقيقة والرسوم الأساسية لكل نوع خدمة.
- **التقارير والإحصائيات**: نظرة عامة على أداء الخدمة.
- **إعدادات الموقع**: تحكم كامل في نصوص الموقع، معلومات الاتصال، وSEO.

## 🛠️ التقنيات المستخدمة

- **الواجهة الأمامية (Frontend)**:
  - HTML5, CSS3
  - Tailwind CSS (لتصميم عصري وسريع الاستجابة)
  - JavaScript (للتفاعلية وحسابات المسافة)
  - FontAwesome (للأيقونات)

- **الواجهة الخلفية (Backend)**:
  - PHP (Native)
  - MySQL (قاعدة البيانات)

## 🚀 طريقة التثبيت والتشغيل

1. **استنساخ المستودع (Clone)**:
   ```bash
   git clone https://github.com/yourusername/taxi-oran-project.git
   ```

2. **إعداد الخادم**:
   - تأكد من تثبيت خادم محلي مثل XAMPP أو WAMP.
   - انقل مجلد المشروع إلى دليل `htdocs` (في XAMPP) أو `www` (في WAMP).

3. **إعداد قاعدة البيانات**:
   - قم بإنشاء قاعدة بيانات جديدة في phpMyAdmin (مثلاً: `taxi_db`).
   - قم باستيراد ملف قاعدة البيانات (`database.sql`) الموجود في المجلد الرئيسي أو مجلد `config` (إن وجد).

4. **ربط قاعدة البيانات**:
   - افتح ملف `config/database.php`.
   - قم بتعديل إعدادات الاتصال لتناسب خادفك المحلي:
     ```php
     define('DB_HOST', 'localhost');
     define('DB_USER', 'root');
     define('DB_PASS', '');
     define('DB_NAME', 'taxi_db');
     ```

5. **التشغيل**:
   - افتح المتصفح واذهب إلى: `http://localhost/dispatch-website`

## 📂 هيكلية المشروع

- `index.php`: الصفحة الرئيسية.
- `booking.php`: صفحة الحجز.
- `admin/`: لوحة تحكم المسؤول.
- `config/`: ملفات الاتصال بقاعدة البيانات والإعدادات.
- `includes/`: ملفات PHP المشتركة (رأس الصفحة، التذييل).
- `assets/` أو `uploads/`: الصور والملفات الثابتة.

## 🤝 المساهمة

نرحب بمساهماتكم لتطوير المشروع! يرجى اتباع الخطوات التالية:
1. قم بعمل Fork للمشروع.
2. أنشئ فرعاً جديداً لميزتك (`git checkout -b feature/AmazingFeature`).
3. قم بعمل Commit لتغييراتك (`git commit -m 'Add some AmazingFeature'`).
4. ارفع الفرع (`git push origin feature/AmazingFeature`).
5. افتح Pull Request.

## 📄 الترخيص

هذا المشروع مرخص بموجب رخصة [MIT](LICENSE).
